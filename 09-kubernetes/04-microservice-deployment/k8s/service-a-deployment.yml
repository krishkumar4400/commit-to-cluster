apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a-deployment
  labels:
    name: service-a-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      labels:
        app: service-a
    spec:
      containers:
        - image: embarkx/tax-service-a
          name: service-a
          resources:
            requests:
              cpu: "250m"
              memory: "250Mi"
            limits:
              cpu: "500m"
              memory: "500Mi"
          ports:
            - containerPort: 3000
              name: service-a-port
